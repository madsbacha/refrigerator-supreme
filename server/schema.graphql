# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

type Category {
    drinks: [Item]
    id: ID
    name: String
}

type Comment {
    author: User
    id: ID
    item: Item
    replies: [Comment]
    replyTo: Comment
    text: String
}

type Item {
    category: Category
    comments: [Comment]
    commentsCount: Int
    id: ID
    image: String
    myRating: Rating
    name: String
    rating: Float
    ratings: [Rating]
}

type LoginResponse {
    token: String
    user: User
}

type Mutation {
    CreateCategory(name: String!): Category
    CreateComment(itemId: ID!, text: String!): Comment
    CreateItem(categoryId: ID, image: String!, name: String!): Item
    CreateUser(email: String!, password: String!): LoginResponse
    DeleteCategory(id: ID!): Response
    DeleteComment(id: ID!): Response
    DeleteItem(id: ID!): Response
    Login(email: String!, password: String!): LoginResponse
    RateItem(itemId: ID!, rating: Float!): Rating
    UpdateCategory(id: ID!, name: String!): Category
    UpdateItem(categoryId: ID, id: ID!, image: String, name: String): Item
    UpdateUser(password: String!): User
}

type Query {
    comments(after: Int, first: Int, itemId: ID, userId: ID): [Comment]
    drinks(after: Int, first: Int): [Item]
    me: User
    ratings(itemId: ID): [Rating]
}

type Rating {
    id: ID
    item: Item
    rating: Float
    user: User
}

type Response {
    success: Boolean
}

type User {
    comments: [Comment]
    email: String
    id: ID
    ratings: [Rating]
}
